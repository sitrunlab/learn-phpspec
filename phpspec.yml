suites:
  suite:
    namespace: App
    src_path: src
    psr4_prefix: App
    spec_prefix: spec

extensions:
  - PhpSpec\Extension\CodeCoverageExtension

code_coverage:
  format:
    - html
    - clover
  output:
    html: coverage
    clover: coverage.xml
